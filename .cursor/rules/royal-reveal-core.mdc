---
description: Royal Reveal 项目整体协作约定
alwaysApply: true
---

# Royal Reveal 项目总规则

## 技术栈

- 前端是**单文件 `index.html`** + 原生 JS + Tailwind CDN + GSAP Flip，不引入打包工具或大型框架。
- 所有 CSS 写在 `<style>` 标签内，JS 写在 `<script>` 标签内，不拆分为外部文件（除非明确要求）。

## 部署环境

- 默认部署目标是 **Cloudflare Workers 静态资源**，配置文件为 `wrangler.jsonc`，不随意切换平台。
- `assets.directory` 设为 `"."` — 所有相对路径资源（`images/`、`audio/`）在本地与 Workers 上均需能正常访问，不改为绝对 URL。

## 资源与命名

- 图片在 `images/`，音效在 `audio/`，保持路径不变。
- `localStorage` 键名统一沿用现有前缀 `royal_reveal_*`，新增本地持久化数据时同样遵守。

## 用户体验约定

- 移动端**横屏**体验优先，任何改动不能让现有的 `@media (max-height: 480px) and (orientation: landscape)` 等断点失效。
- 不增加阻塞游戏主流程的强制步骤（如必填表单、弹窗链路过深等）。
- 音效与动画修改需保持现有 GSAP + SFX 体系一致，不引入其他动画库。

## 代码风格

- 与现有代码风格保持一致：使用 `let` / `const`，函数命名用 `camelCase`，DOM 操作直接使用原生 API。
- 不添加只是重述代码逻辑的注释，注释只用于解释非显而易见的设计决策。
